<ul id="lists">
  <% @lists.each_with_index do |list, index| %>
    <li class>
      <a href="/lists/<%= index %>">
        <h2><%= list[:name] %></h2>
        <p><%= list[:todos].size %></p>
      </a>
    </li>
  <% end %>
</ul>

<a href="/lists/new">Add new list</a>
